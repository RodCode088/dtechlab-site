{
  "name": "dtechlab-launch-site",
  "private": true,
  "version": "1.0.0",
  "description": "DTechLab — sitio de lanzamiento estático",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && python -m http.server 4173 --directory dist",
    "preview": "python -m http.server 4173 --directory dist"
  },
  "engines": { "node": ">=18" }
}
